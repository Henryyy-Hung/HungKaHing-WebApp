import "../globals.css";
import styles from "./layout.module.css";
import localFont from 'next/font/local';
import Link from "next/link";
import Image from "next/image";

const satoshiVariable = localFont({ src: '../../assets/fonts/Satoshi/Satoshi-Variable.woff2' });

const metadata = {
    title: "Henry's Space",
    description: "Generated by create next app",
};

export async function generateStaticParams() {
    return [{ lang: 'zh' }, { lang: 'en' }]
}

const RootLayout = ({ children, params }) => {

    const navItems = [
        {
            title: "Home",
            url: "/"
        },
        {
            title: "Resume",
            url: "/resume"
        },
        {
            title: "Projects",
            url: "/projects"
        },
        {
            title: "Blog",
            url: "/blogs"
        },
        {
            title: "Contact",
            url: "/contact"
        }
    ];

    return (
        <html lang={params.lang}>
            <body className={satoshiVariable.className}>

                <header className={styles.header}>

                    <Link className={styles.logo} href={`${params.lang}`} scroll={false}>
                        <h1>{metadata.title}</h1>
                    </Link>

                    <nav className={styles.nav}>
                        {
                            navItems.map((item, index) => (
                                <span key={index}>
                                    <Link href={`/${params.lang}${item.url}`} key={index} scroll={false}>
                                        {item.title}
                                    </Link>
                                    {
                                        index < navItems.length - 1 && <>&nbsp;&nbsp;|&nbsp;&nbsp;</>
                                    }
                                </span>
                            ))
                        }
                    </nav>
                </header>

                <main className={styles.main}>
                    {children}
                </main>

                <footer className={styles.footer}>
                    <section className={styles.contact}>
                        <div>
                            <h5>Email</h5>
                            <Link href="mailto:henry.k.h.hung@gmail.com">henry.k.h.hung@gmail.com</Link>
                        </div>
                        <div>
                            <h5>Location</h5>
                            <span>Hong Kong / Shanghai</span>
                        </div>
                        <div>
                            <h5>Social</h5>
                            <Link href="https://linkedin.com/in/hungkahing" target={"_blank"}>LinkedIn</Link>
                            &nbsp;|&nbsp;
                            <Link href="https://github.com/Henryyy-Hung" target={"_blank"}>GitHub</Link>
                        </div>
                    </section>

                    <section className={styles.copyright}>
                        <p>© 2024 by Henry Hung - All Rights Reserved</p>
                    </section>
                </footer>
            </body>
        </html>
    );
};

export { metadata };

export default RootLayout;